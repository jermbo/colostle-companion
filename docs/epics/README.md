# Epics

**Altitude:** sparse. A small set of files that anchor **why** we are building; no code.

- Create epics rarely; split only when missions are genuinely distinct.
- Link downward to **`docs/features/`** and **`docs/milestones/<version>/`**; do not restate Feature architecture here.

Starter: copy [`../templates/epic.md`](../templates/epic.md).
