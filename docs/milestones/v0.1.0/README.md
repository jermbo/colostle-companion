# Milestone v0.1.0 (placeholder)

This folder exists so the **`docs/milestones/<semver>/`** pattern is visible in-repo.

Replace or duplicate this semver when your first **real** cut is named; then colocate **`US-*`**, **`T-*`**, and **`US-*-decisions.md`** files here.

Suggested first files once work exists:

- `US-001-….md`
- `US-001-decisions.md` — optional; add when collaborative Q&A warrants it
- `T-001-….md`

Link each story line to **`docs/features/`** Epics/features via YAML frontmatter (see templates).
